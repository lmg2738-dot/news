# 기본: 저장소 루트 config.json 사용 (커밋됨)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
NEWS_KEYWORDS=CJ

# /api/cron HTTP 호출 보호
CRON_SECRET=random_long_string

# --- 저장 (Blob 대신 GitHub JSON) ---
# 웹에서 읽는 URL (기본: raw.githubusercontent.com/.../data/news-state.json)
# STATE_JSON_URL=

# Vercel /api/cron에서 GitHub에 직접 저장 시 (repo 쓰기 권한 PAT)
# GITHUB_TOKEN=
# GITHUB_REPOSITORY=lmg2738-dot/news
# STATE_BRANCH=main

# batch:remote
APP_URL=
