# 기본: 저장소 루트 config.json 사용 (커밋됨)
# 환경 변수가 있으면 config.json보다 우선합니다.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# 쉼표로 여러 키워드 (기본: CJ)
NEWS_KEYWORDS=CJ

# Vercel Cron 보호 (배포 시 자동 생성되는 경우도 있음)
CRON_SECRET=random_long_string

# Storage → Blob 연결 시 Vercel이 자동 주입
BLOB_READ_WRITE_TOKEN=
